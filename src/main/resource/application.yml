spring:
  #数据库配置
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/keshe?useSSL=false&serverTimezone%2B8
    username: root
    password: root

logging:
  level:
    root: warn
    com.mp.dao: trace
  pattern:
    console: '%p%m%n'

